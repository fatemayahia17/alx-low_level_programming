#include "main.h"
/**
 *_isupper - check if input is uper or lower.
 *@c: input to check
 * Return: Always 0.
 */
int _isupper(int c)
{
	int i;

	if (i >= 65 && i <= 90)
		return (1);
	else
		return (0);
}
