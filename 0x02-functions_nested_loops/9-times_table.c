#include "main.h"
#include <stdio.h>
/**
 * times_table - Function caller
 *
 * Return: Always 0 (Success)
 */
void times_table(void)
{
	int i;

	for (i = 0; i <= 10; i++)
	{i
		printf("9 x %d = %d\n", i, 9 * i);
	}
}
