#include "main.h"
/**
 * add - function that prints the summation
 * @a: checks input of function
 * @b: checks input of function
 * Return:  Always 0 (Successed)
 */

int add(int a, int b)
{
	int sum;

	sum = a + b;

	return (sum);
}
