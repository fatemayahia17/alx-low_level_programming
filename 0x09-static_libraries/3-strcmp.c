#include "main.h"
#include <string.h>

/**
 * _strcmp - function that compares two strings.
 * @s1: first parameter
 * @s2: second paramter
 * Return: on success(0)
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
